import Vue from 'vue'
import Router from 'vue-router'

Vue.use(Router)

export default new Router({
  mode: 'hash',
  routes: [
    // {
    //   name: 'example',
    //   // component: () => import('../components/hellWorld.vue')
    // }
  ]
})